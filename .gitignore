# Node modules
node_modules/

# Fichiers de log
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Fichiers système (macOS, Windows, etc.)
.DS_Store
Thumbs.db

# Fichiers d'environnement (ne pas versionner vos secrets)
.env
.env.*

# Dossier de couverture de tests
coverage/
lcov.info

# Dossier de build / distribution (si applicable)
dist/
build/

# Fichiers compilés TypeScript (généralement générés lors de la transpilation)
*.js
*.js.map

# Fichiers de lock package
# (Garde package.json et package-lock.json sous versionnement, si tu souhaites suivre les versions des dépendances)
# package-lock.json

# IDE et éditeurs
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Fichiers temporaires
tmp/
temp/
